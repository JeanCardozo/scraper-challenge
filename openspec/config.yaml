# openspec/config.yaml
# Auto-generated by sdd-init on 2026-06-27
schema: spec-driven

context: |
  Project: scraper-challenge
  Stack: undetected — project is empty, no package.json, go.mod, pyproject.toml, or source files found
  Architecture: not yet established
  Testing: none detected — no test runner, linter, type checker, or formatter installed
  Style: not yet established
  Persistence mode: openspec (file-based)

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns once established
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false  # No test runner detected — cannot enforce TDD
  runner: none
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  linter: none
  type_checker: none
  formatter: none
